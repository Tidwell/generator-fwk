language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-fwk' ]; then cd .. && eval "mv $currentfolder generator-fwk" && cd generator-fwk; fi

